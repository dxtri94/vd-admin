/**
 * In this module we define strings
 * that are going to be used throughout
 * the application in various places.
 * We use objects so that we can refer
 * to them using dot (.) notation.
 */

angular.module('app.labels', [])

.constant('labels', {
    version: "Version 1.0.0",
    copyright: "Copyright"
});
